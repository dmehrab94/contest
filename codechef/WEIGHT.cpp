#include <bits/stdc++.h>
using namespace std;
int main(){
	cout<<"1\n";
	flush(cout);
	cout<<"3 1 1 2\n";
	flush(cout);
	cout<<"3 3 3 4\n";
	flush(cout);
	int a;
	cin>>a;
	if(a==1){
        cout<<"2\n";
        flush(cout);
        cout<<"2\n";
        flush(cout);
	}
	else if(a==2){
        cout<<"2\n";
        flush(cout);
        cout<<"1\n";
        flush(cout);
	}
	else if(a==-1){
        cout<<"2\n";
        flush(cout);
        cout<<"4\n";
        flush(cout);
	}
	else if(a==-2){
        cout<<"2\n";
        flush(cout);
        cout<<"3\n";
        flush(cout);
	}
	else{
        cout<<"2\n";
        flush(cout);
        cout<<"5\n";
        flush(cout);
	}

	return 0;
}
